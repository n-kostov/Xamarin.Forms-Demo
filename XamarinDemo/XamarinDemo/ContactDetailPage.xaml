<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinDemo.ContactDetailPage">
    <StackLayout Padding="5">
        <StackLayout Orientation="Horizontal">
            <Label Text="DisplayName" 
                HorizontalOptions="Start" />
            <Label Text="{Binding DisplayName}" HorizontalOptions="EndAndExpand" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Name" 
                HorizontalOptions="Start" />
            <Label Text="{Binding Name}" HorizontalOptions="EndAndExpand" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Description" 
                HorizontalOptions="Start" />
            <Label Text="{Binding Description}" HorizontalOptions="EndAndExpand" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Phone Number" 
                HorizontalOptions="Start" />
            <Label Text="{Binding PhoneNumber}" HorizontalOptions="EndAndExpand" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="Date of Birth" 
                HorizontalOptions="Start" />
            <Label Text="{Binding DateOfBirth, StringFormat='{0:dd/MMM/yyyy}'}" HorizontalOptions="EndAndExpand" />
        </StackLayout>
    </StackLayout>
</ContentPage>